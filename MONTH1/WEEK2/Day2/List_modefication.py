# Add an element at a specific position (without .insert())
# Remove all occurrences of a given element from a list
# Remove duplicates from a list while preserving order
# Swap the first and last elements of a list
# Replace every negative number in a list with 0
# Merge two lists into one, alternating elements (a1, b1, a2, b2...)

